# Project-local Codex settings.
#
# Codex loads this file only after the project is trusted. Keep it focused on
# repo defaults and leave personal preferences in ~/.codex/config.toml.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

[features]
hooks = true
multi_agent = true
shell_snapshot = true

