# Claude Code Project Config

This directory contains project-local Claude Code customization.

- `agents/` holds project-specific subagent definitions.
- `commands/` holds project-specific slash command definitions.
- `skills` is a symlink to `../skills`, so skills can be edited from the repository root and exposed through the `.claude` layout.

