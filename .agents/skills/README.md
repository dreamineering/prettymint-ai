# Codex Skills

Codex documents repo-specific skills under `.agents/skills`.

This repository keeps the editable source of skills in `skills/` so the same
content can also be exposed through `.claude/skills`. Add or edit skills in
`skills/<skill-name>/`.

