# Skills

Author reusable skills here. Each skill should live in its own directory:

```text
skills/<skill-name>/
  SKILL.md
  scripts/
  references/
```

The `.claude/skills` path points here via symlink for Claude Code compatibility.

